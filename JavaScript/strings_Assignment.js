function myFunction(x, y) {
    var firstName = document.forms["myForm"]["fname"].value;
    var lastName = document.forms["myForm"]["lname"].value;

    // Below is 4 a)
    alert(x + y);

    // Now what does this do and why do you think it happens?

}